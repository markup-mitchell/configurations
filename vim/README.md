# All of these configs and notes assume I am using Arch

* install vundle (or vundle-git) manually
* install vim-airline and powerline-fonts
* run PluginInstall
* themes (*.vim) go in ~/.vim/colors

... everything else handled in .vimrc ???
